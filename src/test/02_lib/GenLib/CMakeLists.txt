GET_DIR_NAME(DIR_NAME)
SET(TARGET_NAME "${DIR_NAME}")

GLOBAL_GROUP_SOURCES(RST SOURCES PATHS
        ${CMAKE_CURRENT_SOURCE_DIR}
        "${PROJECT_SOURCE_DIR}/include/test_02_lib"
)

ADD_TARGET(MODE "LIB" NAME "${TARGET_NAME}" SOURCES ${SOURCES})